# ─── Backend API ─────────────────────────────────────────────────────────────
# Set to "false" (or remove) to point at local backend instead of the hosted one
VITE_IS_PROD=false

# URL of the Node.js backend (used when VITE_IS_PROD=false)
VITE_API_URL=http://localhost:8000
